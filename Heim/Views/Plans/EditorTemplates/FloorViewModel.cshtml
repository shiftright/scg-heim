@model ShiftRight.Heim.Models.FloorViewModel
@{

	string mode = "create";
	var view = ViewContext.View as System.Web.Mvc.RazorView;
	if(view != null) {
		mode = Path.GetFileNameWithoutExtension(view.ViewPath).ToLower();
	}
}

<div class="editor-field">
	@Html.ActionLink("Floor " + @Model.FloorNumber, "Variants", new { ID = Model.ID })
</div>