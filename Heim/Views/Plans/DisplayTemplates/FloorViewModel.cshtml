@model ShiftRight.Heim.Models.Floor
@{

	string mode = "create";
	var view = ViewContext.View as System.Web.Mvc.RazorView;
	if(view != null) {
		mode = Path.GetFileNameWithoutExtension(view.ViewPath).ToLower();
	}
}

<div class="editor-field">
    @Html.ActionLink("Delete", "Delete", new { id = Model.ID }) |
	@Html.ActionLink(String.Format("Floor {0}", Model.FloorNumber), "Floor", new { id = Model.ID })
</div>
<div class="break"></div>